# 人工验收清单

以下项目已完成代码级/后端验证，但需要你手动在浏览器中确认。

## P0 遗留
- [ ] **P0-2 边界情况**：搜索包含异体字、多余空格、换行差异的查询词，确认阅读器仍能正确定位（当前只验证了常规文本）。
- [ ] **P0-3 图谱失败态**：构造一次图谱生成失败（例如查询一个语料库里几乎没有命中的生僻词），确认错误提示清晰、不影响分层分析展示。
- [ ] **P0-3 图谱空状态**：确认图谱为空时有明确提示，而不是一片空白。
- [ ] **P0-3 移动端**：在窄屏/手机浏览器下测试分层分析↔关系图谱切换，图谱区域是否可正常缩放/拖动。
- [ ] **P0-3 视觉一致性**：图谱当前是深色主题，与站点整体暖色浅色风格不统一——确认是否需要调整（未修复，见下方已知问题）。

## P1（本轮新增，待你验收）
- [ ] **图谱节点点击 → 跳转阅读器**：点击图谱中一个"命中"节点（有 dhamma_lineage_id 的文本节点，不是关系图谱里的人物/概念节点），确认信息面板出现"在阅读器中查看原文"按钮，点击后跳转到对应文档的阅读器页面。注意：图谱节点代表整篇文档，跳转不带精确的字符级高亮定位（该文档若恰好是本次搜索命中之一，可在阅读器内自行用页面内查找定位）。
- [ ] 论坛点赞/取消点赞：连续快速点击、多用户并发、刷新后状态保持。
- [ ] 论坛浏览计数：进详情页 vs 列表页曝光，回复后是否误加。
- [ ] **论坛列表新样式**：确认浏览/点赞/回复三个统计框样式统一、并排显示在标题区右侧，"最近活跃"文字移到了行的最右侧。
- [ ] 博客标签：创建/编辑文章打标签、首页按标签筛选、URL 分享后能恢复筛选状态。
- [ ] **搜索计时器**：正常完成、请求失败、网络超时（可用浏览器开发者工具模拟弱网/离线触发 90 秒超时）三种情况下计时器是否都正确停止，超时是否显示清晰提示。
- [x] 复制 Markdown 按钮内容是否为简体、是否为原始 Markdown 而非页面渲染后的纯文本——已确认代码路径正确（复制的是 `data.synthesis` 原文转简体，非 DOM 文本），建议仍手动点一次确认。
- [ ] **"总结与结论"卡片**：确认无论 AI 生成的总结标题措辞如何变化，都能被正确识别为独立卡片，不会被并入最后一层的分析卡片。
- [x] 破折号渲染：区块交界处的"—"消失，正文/引用内部的"—"保留——已确认代码路径正确，建议仍手动看一次实际渲染效果。

## 已知问题（暂未修复）
- 图谱深色主题与站点风格不统一（视觉，非功能性）。

## 管理员权限 / 额度申请（本轮新增，待你验收）
- [ ] 登录管理员账号 → 个人资料页出现"成员管理"标签页 → 能看到全部成员列表。
- [ ] 将一个普通成员设为管理员，确认对方角色变化后能访问 AI 无限额度和管理功能。
- [ ] 尝试把最后一个管理员降级为成员，确认后端拒绝（"Cannot remove the last remaining admin"）。
- [ ] 普通成员账号 → AI 额度页 → 提交一条额度申请（带备注），确认提交后按钮变为"取消申请"、且无法重复提交第二条待处理申请。
- [ ] 管理员在"成员管理"页看到该条待处理申请，批准并填写金额，确认成员端余额到账、状态变为"已批准"。
- [ ] 管理员拒绝一条申请，确认成员端状态正确显示"已拒绝"。
- [ ] 成员取消一条待处理申请，确认状态变为"已取消"且可以重新提交。

## 问问乔达摩先生（尚未开始）
完成后会在此清单追加对应验收项。
